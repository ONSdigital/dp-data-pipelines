# `tests` Directory

This directory contains the `unit tests` and the required `test-cases`.

`unit tests` are small test to verify each function is working ad=s intended.

`test-cases` are test files that are used to run unit tests.